# Docker

This folder contains the configuration files for containerizing the application.

## Responsibility
- **docker-compose.yml**: Defines the multi-container environment (App, MongoDB).
- **Dockerfile**: (If present) contains the build instructions for the application container.
- **.env**: (If present) localized environment variables for the docker environment.
